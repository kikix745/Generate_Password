# Generate_Password

This is a simple implementation to generate random codes 

"The important thing to improve is the security,because random is predictable "
      -------------------------------------------------------------------------
      
      so the solution is using secrets bib_ which provide the passwords
      
      -------------------------------------------------------------------------
